{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://greentic.ai/schemas/messaging/ops/send.input.schema.json",
  "title": "SendInput",
  "description": "Input contract for provider-core send operation.",
  "type": "object",
  "required": ["to"],
  "additionalProperties": false,
  "properties": {
    "to": {
      "type": "string",
      "minLength": 1,
      "description": "Target recipient (channel, room, or address)."
    },
    "text": {
      "type": ["string", "null"],
      "minLength": 1,
      "description": "Text content to send."
    },
    "attachments": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/attachment"
      },
      "default": [],
      "description": "Optional inline attachments encoded as base64."
    },
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional routing hints.",
      "properties": {
        "thread_id": {
          "type": "string",
          "minLength": 1
        },
        "reply_to": {
          "type": "string",
          "minLength": 1
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": []
        }
      }
    }
  },
  "allOf": [
    {
      "anyOf": [
        {
          "required": ["text"]
        },
        {
          "properties": {
            "attachments": {
              "minItems": 1
            }
          }
        }
      ],
      "description": "Require either text or at least one attachment."
    }
  ],
  "$defs": {
    "attachment": {
      "type": "object",
      "required": ["name", "content_type", "data_base64"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "content_type": {
          "type": "string",
          "minLength": 1
        },
        "data_base64": {
          "type": "string",
          "contentEncoding": "base64",
          "minLength": 1
        }
      }
    }
  }
}
