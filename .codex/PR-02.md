---
id: PR-02
track: host
depends_on: [PR-01]
---
# PR-02: Define RenderTier + RenderPlan core types (no behavior change)

## Goal
Introduce host-level “render planning” types in a new module/crate without changing runtime behavior.

## Changes
- Add `RenderTier` enum (A/B/C/D) and `RenderPlan` struct
- Add `RenderWarning` struct/enum for deterministic warnings
- Add serde derives; add schemars derives if your ecosystem uses it

## Suggested location
Choose one (prefer the one that matches existing structure):
- `crates/messaging-render-core/` (new crate), OR
- `crates/greentic-messaging-core/src/render_plan.rs` (existing core crate)

## Types (minimum)
- `RenderTier::{TierA, TierB, TierC, TierD}`
- `RenderPlan { tier, summary_text, actions, attachments, warnings, debug }`
- `RenderWarning { code, message, path }`

## Acceptance criteria
- Builds everywhere
- No code paths changed in ingress/egress
- Unit test: serialize/deserialize a `RenderPlan` JSON snapshot

## Notes
- Keep it generic: no provider names, no Teams/Telegram-specific fields.
