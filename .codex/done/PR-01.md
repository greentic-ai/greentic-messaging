---
id: PR-01
track: host
depends_on: []
---
# PR-01: Add `.codex` migration scaffolding + PR runner

## Goal
Set up the DWBase-style migration workflow inside `greentic-messaging` so we can execute small PRs sequentially and safely.

## Changes
- Add `.codex/STATE.json` (repo_hint, default_track, done list)
- Add `scripts/codex_next.py` (the runner you shared, with optional track/deps support if you want it)
- Add `.codex/README.md` describing how to use `codex_next.py`

## Files
- `.codex/STATE.json`
- `.codex/README.md`
- `scripts/codex_next.py`

## Acceptance criteria
- `python3 scripts/codex_next.py --status` works
- `python3 scripts/codex_next.py` prints the next PR markdown
- State updates correctly on `--done PR-01`
- CI stays green

## Notes
- Keep this PR strictly additive. No refactors yet.
